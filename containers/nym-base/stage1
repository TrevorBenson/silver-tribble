FROM centos:centos8.3.2011

RUN dnf -y install epel-release && \
    curl --proto '=https' --tlsv1.2 https://sh.rustup.rs -sSf | sh -s -- -y && \
    ~/.cargo/bin/rustup -y update && \
    yum -y clean all && \
    rm -rf /var/cache/yum
