FROM ubuntu:20.04

RUN apt-get update && \
    apt-get install -y pkg-config build-essential libssl-dev curl jq && \
    rm -rf /var/lib/apt/lists/*

RUN curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh && \
    rustup update
