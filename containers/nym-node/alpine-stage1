FROM chaincrucial/nym-base:alpine-rust-stage1

RUN rustup update && \
    git clone https://github.com/nymtech/nym.git && \
    cd nym && \
    git checkout tags/v0.10.0 && \
    cargo build --release
